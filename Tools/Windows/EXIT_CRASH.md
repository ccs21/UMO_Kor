# PC 종료 크래시 점검 (2026-09-04)

## 확인된 내용

- 기존 PC 빌드 종료 시 UnityPlayer.dll 접근 위반 `0xc0000005` 기록이 있습니다.
- 읽으려던 주소는 `0x50`이며, 호출 기록 하단에는 Mono의 `mono_threads_set_shutting_down`이 있습니다.
- 엔진/관리 코드 프레임 일부에 심볼이 없어 정확한 원인 함수는 확정하지 못했습니다. 메모리 부족이나 세이브 손상으로 단정할 근거는 없습니다.
- 이 문제는 beta.5의 한글 폰트 수정과 별개이며 이번 수정으로 해결되었다고 보장하지 않습니다.

## 추가 확인 대상

- 종료 시 관리 객체 정리 및 Unity API 호출 순서.
- `CriMana.Player` finalizer는 Dispose 경로에서 Unity 객체 접근을 할 수 있습니다. 실제 크래시의 해당 프레임인지 확인이 필요합니다.
- VLC 관리자와 재생 객체의 종료/해제 순서도 점검 대상이지만, DLL이 로드되어 있다는 사실만으로 VLC를 원인으로 판단할 수 없습니다.
- 별도 검수 프로필로 메인 화면 및 영상/악곡 재생 후 정상 종료를 비교하고, 심볼이 있는 스택으로 범위를 좁혀야 합니다.

원본 세이브 및 크래시 덤프는 저장소에 포함하지 않습니다.

## 진단 빌드

beta.6 소스의 Windows 독립 실행 빌드는 기존 저장 루트에 `pc-shutdown-trace.log`를 추가합니다. 세이브를 수정하지 않습니다.
실행 시작·종료 요청과 주요 finalizer의 ENTER/EXIT, 시각 및 스레드 번호를 기록합니다. Unity 로그 API를 호출하지 않는 파일 기록입니다.
다음 크래시의 output_log.txt/덤프 시각과 같은 실행 구간을 대조하세요. ENTER만 남은 항목은 추가 조사 후보이며, 그 자체만으로 원인 확정은 아닙니다.
기록은 누적되며 Android와 에디터에서는 활성화되지 않습니다.

## 2026-09-05 재현 및 수정

- 00:26:58 KST 종료 요청 후, GC 스레드의 `CriAtomExOutputAnalyzer finalizer ENTER`까지만 기록되고 EXIT 없이 접근 위반이 발생했습니다. 이전과 같은 UnityPlayer.dll 위치에서 주소 `0x50`을 읽다 실패했습니다.
- 해당 정리 경로는 플레이어 상태 조회/정지 및 TodoLogger를 통한 RuntimeSettings 접근을 수행할 수 있습니다. 심볼 없는 덤프만으로 그중 어느 호출에서 실패했는지는 확정하지 못했습니다.
- Windows 독립 실행판의 분석기 핸들은 실제 네이티브 자원이 아닌 `IntPtr(1)`입니다. 이 플랫폼의 finalizer는 핸들과 관리 참조만 비우도록 변경했습니다. 명시적 Dispose는 기존 해제 경로와 finalizer 억제를 유지합니다. Android 및 에디터는 변경하지 않습니다.
- 수정 후 악곡 재생·메뉴 이용 뒤 여러 차례 정상 종료 여부를 확인해야 합니다. 이 변경만으로 다른 종료 오류까지 해결되었다고 보장하지 않습니다.
